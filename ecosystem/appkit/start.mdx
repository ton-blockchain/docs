---
title: "Start with AppKit"
sidebarTitle: "Start"
icon: rocket
---

Use this section to add AppKit to a new application and send the first wallet request.

## Short path

1. [Install packages](/ecosystem/appkit/start/installation).
1. [Create an AppKit instance](/ecosystem/appkit/start/configuration).
1. [Connect a wallet](/ecosystem/appkit/start/wallet-connection).
1. [Send a first transaction](/ecosystem/appkit/start/first-transaction).
1. [Prepare for production](/ecosystem/appkit/start/production).

## Minimum integration

```tsx
import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
import { AppKit, Network, createTonConnectConnector } from '@ton/appkit';
import { AppKitProvider } from '@ton/appkit-react';
import '@ton/appkit-react/styles.css';

const appKit = new AppKit({
  networks: {
    [Network.mainnet().chainId]: {
      apiClient: { url: 'https://toncenter.com' },
    },
  },
  connectors: [
    createTonConnectConnector({
      tonConnectOptions: { manifestUrl: 'https://example.com/tonconnect-manifest.json' },
    }),
  ],
});

const queryClient = new QueryClient();

export function Root() {
  return (
    <QueryClientProvider client={queryClient}>
      <AppKitProvider appKit={appKit}>{/* app */}</AppKitProvider>
    </QueryClientProvider>
  );
}
```

## Next pages

- [Quick start](/ecosystem/appkit/start/quick-start) gives a copyable React setup.
- [Configuration](/ecosystem/appkit/start/configuration) explains `AppKitConfig`.
- [Production](/ecosystem/appkit/start/production) lists the checks required before mainnet traffic.
