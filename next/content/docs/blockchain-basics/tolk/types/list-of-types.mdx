---
id: tolk/types/list-of-types
title: "Type system overview"
sidebarTitle: "List of types"
description: ""
---

Tolk has the following types:

- [numbers](/blockchain-basics/tolk/types/numbers) — `int`, `int32`, `uint64`, `coins`, and others.
- [boolean](/blockchain-basics/tolk/types/booleans) — `true` and `false`.
- [address](/blockchain-basics/tolk/types/address) — `internal`, `external`, and `none`.
- [cells](/blockchain-basics/tolk/types/cells) — containers with up to 1023 bits of data and up to 4 references to other cells, plus the cell manipulation primitives of TVM: [builders and slices](/blockchain-basics/tvm/builders-and-slices).
- [structures](/blockchain-basics/tolk/types/structures) — multiple fields grouped into one entity.
- [generics](/blockchain-basics/tolk/types/generics) — any struct can be generic `<T>`.
- [enums](/blockchain-basics/tolk/types/enums) — distinct types containing integer variants.
- [nullable types](/blockchain-basics/tolk/types/nullable) — `null` safety and safe casts.
- [union types](/blockchain-basics/tolk/types/unions) — variables holding one of several possible values.
- [strings](/blockchain-basics/tolk/types/strings) — string values stored as snake-encoded cells.
- [tensors](/blockchain-basics/tolk/types/tensors) — multiple values placed sequentially on the stack.
- [arrays](/blockchain-basics/tolk/types/tuples) — dynamically sized containers backed by TVM tuples.
- [maps](/blockchain-basics/tolk/types/maps) — key-value dictionaries.
- [callables](/blockchain-basics/tolk/types/callables) — first-class functions.
- [unknown](/blockchain-basics/tolk/types/unknown) — any value that is a single-slot TVM primitive.
- [`void` and `never`](/blockchain-basics/tolk/types/void-never) — both represent the absence of a value.

To make an interchangeable alias for an existing type, including structural types, there are [type aliases](/blockchain-basics/tolk/types/aliases).

Smart contracts run on a stack-based virtual machine, [TVM](/blockchain-basics/tvm/overview), which imposes specific rules on how values are represented at runtime. For example, [strings](/blockchain-basics/tolk/types/strings) are stored as snake-encoded cells, because TVM has no native string support.

All on-chain data and communication rely entirely on [cells](/blockchain-basics/tolk/types/cells), so the type system focuses on binary serialization and clear data relationships:

- [Type checks and casts](/blockchain-basics/tolk/types/type-checks-and-casts) covers casting with the unsafe `as` operator.
- [TVM stack representation](/blockchain-basics/tolk/types/overall-tvm-stack) summarizes how types map to the TVM stack.
- [Serialization](/blockchain-basics/tolk/types/overall-serialization) describes how types serialize and relate to [TL-B](/blockchain-basics/languages/tl-b/overview).
